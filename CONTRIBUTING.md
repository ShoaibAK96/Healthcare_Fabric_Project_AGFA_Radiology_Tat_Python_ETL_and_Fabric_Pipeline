# Contributing

1. Create a feature branch.
2. Keep environment-specific values outside source control.
3. Use synthetic data in tests and examples.
4. Run `pytest` and `python scripts/validate_repository.py`.
5. Explain architectural or schema changes in the pull request.

Changes affecting the public data contract should include tests and an update to the relevant documentation.
